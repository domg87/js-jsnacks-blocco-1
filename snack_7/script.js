// Crea un array vuoto. Chiedi per 6 volte all’utente di inserire
// un numero, se è dispari inseriscilo nell’array.


// 1. creiamo un array vuoto
var list = [];

// 2. chiedi all'utente di inserire un numero per 6 volte
var utenteNum = parseInt(prompt("Inserisci un numero"));

// 3. se è dispari inseriscilo nell'array
var i = 0;

while (i < 6) {
  if(utenteNum % 2 != 0);
  list.push(utenteNum);
  i++;
}

for(var i = 0; i < 6; i++){
  if(utenteNum == dispari);
  inner.HTML
}
